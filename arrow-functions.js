// Coming next

const arrowFunctionWithArgumentAndReturnValue = (x) => x;

const arrowFunctionWithBody = () => {};

const arrowFunctionWithArgumentBodyAndReturnValue = (x) => {
  const double = 2 * x;

  return double - 100;
};

const multiply = (x, y) => x * y;
